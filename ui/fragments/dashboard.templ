package fragments

import "goweb/db/sections"

templ Dashboard(sectionList []sections.DataModel) {
  <div>
    <h1>Dashboard</h1>
    for _, section := range sectionList {
      <label>{ section.Title }</label>
    }
  </div>
}
